# Database
DATABASE_URL="file:./dev.db"

# App
NEXT_PUBLIC_APP_NAME="Pendataan Karyawan"

# Auth (admin via env)
# Generate dengan: openssl rand -base64 32
AUTH_SECRET=
ADMIN_EMAIL="admin@example.com"
# Hash bcrypt: node -e "require('bcryptjs').hash('admin123',10).then(h=>console.log(h))"
ADMIN_PASSWORD_HASH=
